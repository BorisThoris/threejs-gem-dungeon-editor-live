/** Frame-rate observations, like the existing draft and player-position data.
 * Colonies own their entries; the HUD polls without putting animation in Zustand. */
export const bellcapWarnings = new Map<string, string>();
